"use client";

export const GameFilters = () => {
    return (
        <header className="py-12 border-b border-light-border-1 w-full">
            <h1 className="text-start font-bold text-4xl text-foreground">
                Top Sellers
            </h1>

            <select name="" id=""></select>
        </header>
    );
};
